########## Server Configuration ##########
spring.datasource.url=jdbc:mysql://localhost:3306/${env.DB_URL}
spring.datasource.username=${env.DB_USER}
spring.datasource.password=${env.DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
########## Hibernate Configuration ##########
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
########## JWT Configuration ##########
spring.security.user.name=${env.SECURITY_USER_NAME}
spring.security.user.password=${env.SECURITY_USER_PASSWORD}
spring.security.user.roles=${env.SECURITY_USER_ROLES}

